{
  "name": "tada-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "cd apps/web && next dev",
    "build": "npm -w packages/shared run build && cd apps/web && next build",
    "lint": "npm -w apps/web run lint"
  }
}
