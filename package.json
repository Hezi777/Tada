{
  "name": "tada-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm -w apps/web run dev\" \"npm -w apps/api run dev\"",
    "build": "npm -w packages/shared run build && npm -w apps/api run build && npm -w apps/web run build",
    "lint": "npm -w apps/web run lint"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
